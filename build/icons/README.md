Place generated platform icons in this folder when preparing signed release builds.

Recommended files:
- `icon.icns` for macOS
- `icon.ico` for Windows
- `256x256.png` and related PNG sizes for Linux

`build/app-icon.svg` is the editable source artwork for generating those assets.
